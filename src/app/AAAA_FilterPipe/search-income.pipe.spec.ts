import { SearchIncomePipe } from './search-income.pipe';

describe('SearchIncomePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIncomePipe();
    expect(pipe).toBeTruthy();
  });
});
