import { MultiSearchIncomePipe } from './multi-search-income.pipe';

describe('MultiSearchIncomePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSearchIncomePipe();
    expect(pipe).toBeTruthy();
  });
});
