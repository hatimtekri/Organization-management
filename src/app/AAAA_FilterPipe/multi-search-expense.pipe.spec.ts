import { MultiSearchExpensePipe } from './multi-search-expense.pipe';

describe('MultiSearchExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSearchExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
