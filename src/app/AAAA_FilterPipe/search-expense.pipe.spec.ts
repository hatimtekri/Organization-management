import { SearchExpensePipe } from './search-expense.pipe';

describe('SearchExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
